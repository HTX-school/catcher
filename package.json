{
  "name": "catcher",
  "version": "0.0.1",
  "description": "Backend server for a tag game",
  "main": "server.js",
  "scripts": {
    "dev": "npx nodemon .",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "William Jelgren",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^10.0.0"
  },
  "dependencies": {
    "socket.io": "^4.3.2"
  }
}
